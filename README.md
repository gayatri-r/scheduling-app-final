Completed Scheduling App frontend

App.js | contains navigation stack and sets the Welcome page as the top in the stack
components/Logout.js | handles users logging out of the app
providers/AuthProvider.js | manages users signing in and out
views/ | contains all screens used in app, see screens for more information